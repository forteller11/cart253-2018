class Ray{
  constructor(angle){
    this.x = mouseX;
    this.y = mouseY;

    this.angle = angle;
    this.vertX[i] = (cos(this.angle[i]) * this.r);
    this.vertY[i] = (sin(this.angle[i]) * this.r);
    /*
    x unit vector,
    y unit vectors
    */
  }
}
